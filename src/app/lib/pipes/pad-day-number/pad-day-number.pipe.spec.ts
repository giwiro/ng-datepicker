import { PadDayNumberPipe } from './pad-day-number.pipe';

describe('PadDayNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new PadDayNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
